# =====================================================================================
# 基础配置 (Basic Configuration)
# =====================================================================================
baseURL = 'https://codersgl.github.io/blog/'
title = "codersgl's blog"
theme = 'hugo-theme-stack'

# 语言与时区
languageCode = "zh-Hans"         # 语言编码：简体中文（中国）
defaultContentLanguage = "zh-cn" # 默认内容语言
timeZone = "Asia/Shanghai"       # 时区：上海
hasCJKLanguage = true            # 中文排版优化

# =====================================================================================
# 输出与链接 (Outputs & Permalinks)
# =====================================================================================
[outputs]
home = ['html', 'rss']
section = ['html']
taxonomy = ['html']
term = ['html']

[pagination]
pagerSize = 8

[taxonomies]
genre = 'categories'
tag = 'tags'

# =====================================================================================
# 缓存与服务 (Caches & Services)
# =====================================================================================
[caches.images]
dir = ':cacheDir/images'

[services]
# [services.disqus]
#     shortname = "your-disqus-shortname"
# [services.googleAnalytics]
#     id = "your-google-analytics-id"

# =====================================================================================
# 标记语言配置 (Markup)
# =====================================================================================
[markup.goldmark.renderer]
unsafe = true

[markup.tableOfContents]
startLevel = 2
endLevel = 4
ordered = false

[markup.highlight]
noClasses = false
codeFences = true
guessSyntax = true
lineNoStart = 1
lineNos = true
lineNumbersInTable = true
tabWidth = 4

# =====================================================================================
# 菜单配置 (Menu)
# =====================================================================================
[menu]
main = [
    { name = '首页', url = '/', weight = 1, identifier = 'home', params = { icon = 'home', newTab = false } },
    { name = '归档', url = '/page/archives', weight = 2, identifier = 'archive', params = { icon = 'archives', newTab = false } },
    { name = '友链', url = '/page/links', weight = 3, identifier = 'links', params = { icon = 'link', newTab = false } },
]

social = [
    { identifier = 'github', name = 'GitHub 主页', url = 'https://github.com/codersgl', params = { icon = 'brand-github', newTab = true } },
    { identifier = 'rss', name = 'RSS 订阅', url = '/index.xml', params = { icon = 'rss', newTab = false } },
]
# =====================================================================================
# 主题参数配置 (Theme Parameters)
# =====================================================================================
[params]
description = "codersgl's blog"
mainSections = ["posts"]
featuredImageField = "image"
featuredImage = true
favicon = '/blog/favicon.ico'

# 配色方案
[params.colorscheme]
toggle = true
default = 'light'

# 顶部导航栏
[params.navbar]
logo = ""
displayTitle = true
displayLogo = false

# 侧边栏
[params.sidebar]
emoji = "🍥"
subtitle = "Imagination is the source of all motivation."

[params.sidebar.avatar]
enabled = true
local = true
src = "images/avatar.jpg"

[params.imageprocessing]
cover = { enabled = false }
content = { enabled = false }

# 文章页面
[params.article]
math = false
toc = true
readingTime = true
[params.article.license]
enabled = true
default = "Licensed under CC BY-NC-SA 4.0"

# 页脚
[params.footer]
displayCopyright = true
copyright = "© 2026 codersgl"

# 日期格式
[params.dateFormat]
published = "Jan 02, 2006"
lastUpdated = "Jan 02, 2006 15:04 MST"

# 小组件 (Widgets)
[params.widgets]
homepage = [
    { type = 'search' },
    { type = 'archives', params = { limit = 10 } },
    { type = 'categories', params = { limit = 10 } },
    { type = 'tag-cloud', params = { limit = 20 } },
]

page = [
    { type = 'search' },
    { type = 'toc' },
    { type = 'categories', params = { limit = 10 } },
    { type = 'tag-cloud', params = { limit = 20 } },
]

# 评论系统 (Comments)
[params.comments]
enabled = true
provider = 'waline'
[params.comments.waline]
serverURL = 'https://comment.shaobin-jiang.com'
lang = 'zh-CN'
emoji = [
    'https://cdn.jsdelivr.net/gh/walinejs/emojis/bmoji',
    'https://cdn.jsdelivr.net/gh/walinejs/emojis/bilibili',
    'https://cdn.jsdelivr.net/gh/walinejs/emojis/tieba',
]
imageUploader = false
requiredMeta = ["nick", "mail"]
